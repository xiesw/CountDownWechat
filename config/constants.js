import {Theme} from "./Theme";

const MARK_COLOR = [
  Theme.color.red,
  Theme.color.yellow,
  Theme.color.blue,
  Theme.color.purple,
  Theme.color.green,
  Theme.color.gray,
];

module.exports = {
  MARK_COLOR: MARK_COLOR
};

